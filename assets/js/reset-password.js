import { api, loadBrand, notify } from './api.js';
document.head.insertAdjacentHTML('beforeend', '<link rel="stylesheet" href="./assets/css/auth.css">');
loadBrand();
const form = document.querySelector('#reset-form'); const emailInput = document.querySelector('#email');
emailInput.value = new URLSearchParams(location.search).get('email') || '';
document.querySelectorAll('.password-toggle').forEach((button) => button.addEventListener('click', () => { const input = document.getElementById(button.dataset.target); input.type = input.type === 'password' ? 'text' : 'password'; button.querySelector('i').className = input.type === 'password' ? 'bi bi-eye' : 'bi bi-eye-slash'; }));
form.addEventListener('submit', async (event) => { event.preventDefault(); if (!form.checkValidity()) return form.reportValidity(); const otp = document.querySelector('#otp').value.replace(/\D/g, ''); const password = document.querySelector('#password').value; const confirmation = document.querySelector('#confirmation').value; if (otp.length !== 6) return notify('Enter the complete 6-digit verification code.', 'error'); if (password !== confirmation) return notify('The passwords do not match.', 'error'); const button = form.querySelector('button[type="submit"]'); button.disabled = true; button.querySelector('span').textContent = 'Resetting password…'; try { const result = await api('/reset-password', { method: 'POST', body: JSON.stringify({ email: emailInput.value.trim(), otp, password, password_confirmation: confirmation }) }); notify(result.message || 'Password reset successfully.', 'success'); setTimeout(() => location.replace('./login.html?reset=1'), 900); } catch (error) { notify(error.message, 'error'); } finally { button.disabled = false; button.querySelector('span').textContent = 'Reset password'; } });
