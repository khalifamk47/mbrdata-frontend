import { api, money } from './api.js';
import { escapeHtml, handleAdminError, initAdminShell, loading, openLegacyModal } from './shell.js?v=20260829-19';
initAdminShell('transactions'); const $=s=>document.querySelector(s); const state={status:new URLSearchParams(location.search).get('status')||'all',page:1};
const meta=s=>String(s)==='1'?['Successful','success']:String(s)==='2'?['Pending','warning']:['Failed','danger'];
async function loadTransactions(){const p=new URLSearchParams({status:state.status,page:state.page,per_page:25,search:$('#search').value,from:$('#from').value,to:$('#to').value});try{const out=await api(`/admin/transactions?${p}`),pager=out.transactions;Object.entries(out.counts||{}).forEach(([k,v])=>document.querySelector(`[data-count="${k}"]`)?.replaceChildren(String(v)));$('#transactions-body').innerHTML=(pager.data||[]).map((t,i)=>{const [label,cls]=meta(t.status);return `<tr><td><input class="transaction-select" type="checkbox" value="${t.id}" aria-label="Select transaction ${escapeHtml(t.transid||t.id)}"></td><td>${i+1+(pager.current_page-1)*pager.per_page}.</td><td>${escapeHtml(t.username||'')}</td><td>${escapeHtml(t.transid||'')}</td><td>${escapeHtml(t.network||'')}</td><td>${escapeHtml(t.service||'')}</td><td>${escapeHtml(t.mobile||'')}</td><td>${escapeHtml(t.plans||'')}</td><td>${escapeHtml(t.type||'')}</td><td>${money(t.amount)}</td><td>${money(t.profit)}</td><td>${escapeHtml(t.date||'')}</td><td><span class="btn btn-${cls} btn-sm">${label}</span></td><td><button class="btn btn-success btn-sm" data-view="${t.id}" title="View"><i class="fa fa-eye"></i></button> <button class="btn btn-danger btn-sm" data-delete="${t.id}" title="Delete"><i class="fa fa-trash"></i></button></td></tr>`}).join('')||'<tr><td colspan="14" class="text-center py-4"><b>No Transaction Made Yet</b></td></tr>';$('#page-info').textContent=`Showing ${pager.from||0} to ${pager.to||0} of ${pager.total} transactions`;$('#prev').disabled=!pager.prev_page_url;$('#next').disabled=!pager.next_page_url;$('#select-all-transactions').checked=false;updateSelection()}catch(e){handleAdminError(e,'Unable to load transactions')}}
async function receipt(id){loading('Loading transaction…');try{const t=(await api(`/admin/transactions/${id}`)).data,[label,cls]=meta(t.status);Swal.close();const field=(name,value)=>`<div class="col-md-6"><div class="form-group"><label>${name}</label><div class="form-control legacy-readonly">${value}</div></div></div>`;openLegacyModal({title:'View Transaction',body:`<div class="row">${field('Username',escapeHtml(t.username||''))}${field('Transaction ID',escapeHtml(t.transid||''))}${field('Network',escapeHtml(t.network||''))}${field('Service',escapeHtml(t.service||''))}${field('Mobile Number',escapeHtml(t.mobile||''))}${field('Plan',escapeHtml(t.plans||''))}${field('Type',escapeHtml(t.type||''))}${field('Amount',money(t.amount))}${field('Profit',money(t.profit))}${field('Old Balance',money(t.oldbal))}${field('New Balance',money(t.newbal))}${field('Date',escapeHtml(t.date||''))}${field('Status',`<span class="badge badge-${cls}">${label}</span>`)}${field('API Response',escapeHtml(t.api_response||''))}</div>`,confirmText:'Update Status',onConfirm:(_host,close)=>{close();manageStatus(id)}})}catch(e){handleAdminError(e)}}
async function manageStatus(id){const r=await Swal.fire({title:'Update transaction',input:'select',inputOptions:{success_only:'Mark successful only',success_debit:'Mark successful and debit user',failed_only:'Mark failed only',failed_refund:'Mark failed and refund user'},inputPlaceholder:'Select an action',showCancelButton:true,confirmButtonColor:'#132b86',inputValidator:v=>!v&&'Select an action'});if(!r.isConfirmed)return;const confirm=await Swal.fire({icon:'warning',title:'Confirm balance action',text:'Some status actions change the customer wallet balance.',showCancelButton:true,confirmButtonText:'Proceed',confirmButtonColor:'#d97706'});if(!confirm.isConfirmed)return;loading('Updating transaction…');try{const out=await api(`/admin/transactions/${id}/status`,{method:'PATCH',body:JSON.stringify({action_type:r.value})});await Swal.fire('Completed',out.message,'success');loadTransactions()}catch(e){handleAdminError(e)}}
async function deleteTx(id){const c=await Swal.fire({icon:'warning',title:'Delete transaction?',text:'The transaction record will be permanently removed.',showCancelButton:true,confirmButtonText:'Delete',confirmButtonColor:'#d33'});if(!c.isConfirmed)return;loading('Deleting…');try{const out=await api(`/admin/transactions/${id}`,{method:'DELETE'});await Swal.fire('Deleted',out.message,'success');loadTransactions()}catch(e){handleAdminError(e)}}
function selectedIds(){return [...document.querySelectorAll('.transaction-select:checked')].map(box=>Number(box.value))}
function updateSelection(){const ids=selectedIds();$('#selected-count').textContent=ids.length;$('#bulk-delete').disabled=!ids.length;const boxes=document.querySelectorAll('.transaction-select');$('#select-all-transactions').checked=boxes.length>0&&ids.length===boxes.length;$('#select-all-transactions').indeterminate=ids.length>0&&ids.length<boxes.length}
async function bulkDelete(){const ids=selectedIds();if(!ids.length)return;const c=await Swal.fire({icon:'warning',title:'Delete selected transactions?',text:`You are about to permanently delete ${ids.length} transaction${ids.length===1?'':'s'}.`,showCancelButton:true,confirmButtonText:'Delete Selected',confirmButtonColor:'#d33'});if(!c.isConfirmed)return;loading('Deleting transactions…');try{const out=await api('/admin/transactions/bulk-delete',{method:'DELETE',body:JSON.stringify({ids})});await Swal.fire({icon:'success',title:out.title||'Deleted',text:out.message,confirmButtonColor:'#132b86'});loadTransactions()}catch(e){handleAdminError(e)}}
document.querySelectorAll('[data-status]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-status]').forEach(x=>x.className='btn btn-light btn-sm');b.className=`btn btn-${b.dataset.status==='all'?'secondary':b.dataset.status==='1'?'success':b.dataset.status==='2'?'warning':'danger'} btn-sm active`;state.status=b.dataset.status;state.page=1;loadTransactions()});$('#filter-transactions').onclick=loadTransactions;$('#reset-transactions').onclick=()=>{$('#search').value='';$('#from').value='';$('#to').value='';loadTransactions()};$('#prev').onclick=()=>{state.page--;loadTransactions()};$('#next').onclick=()=>{state.page++;loadTransactions()};$('#select-all-transactions').onchange=e=>{document.querySelectorAll('.transaction-select').forEach(box=>box.checked=e.target.checked);updateSelection()};$('#bulk-delete').onclick=bulkDelete;$('#transactions-body').onchange=e=>{if(e.target.matches('.transaction-select'))updateSelection()};$('#transactions-body').onclick=e=>{const view=e.target.closest('[data-view]'),del=e.target.closest('[data-delete]');if(view)receipt(view.dataset.view);if(del)deleteTx(del.dataset.delete)};loadTransactions();
